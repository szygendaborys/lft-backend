name: Pull Request

on:
  workflow_dispatch:
  pull_request:
    branches: [develop]

jobs:
  unit-tests:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2

      - name: Install npm packages
        run: npm ci

      - name: Run unit tests
        run: npm run test:unit
